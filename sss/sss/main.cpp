#include "shisanshui.h"
//#include "Curl.h"
string s;
int main() {
	getline(cin, s);
	sss p = sss(s);
	p.doit();
	/*CURL *curl = nullptr;
	CURLcode res;
	curl = curl_easy_init();*/
	return 0;
}